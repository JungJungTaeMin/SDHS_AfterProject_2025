spring.application.name=afterProject
server.port=8083

spring.datasource.url=jdbc:oracle:thin:@localhost:1521/xe
spring.datasource.username=jung
spring.datasource.password=1234
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 1. ?? Hibernate ??? ???? OracleDialect? ?????.
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# 2. SQL ??????(?? ? ??) ??? ??? Oracle(ROWNUM)? ?? ?????.
spring.jpa.properties.hibernate.dialect.pagination.limit_handler=oracle_rownum


# Throw exception when no handler is found
spring.mvc.throw-exception-if-no-handler-found=true
# Disable default static resource handling
spring.web.resources.add-mappings=false